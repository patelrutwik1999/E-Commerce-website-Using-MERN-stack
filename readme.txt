Logic always comes in controller, if we try to write logic in routes file it becomes messier.

Body parser allows us to get all the fields of the form.

npm i express-validator => for validating the required feilds AND outputs a good, understandable error messages.

npm i express-jwt jsonwebtoken => These two libraries are for Json web token for express.